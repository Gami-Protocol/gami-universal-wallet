/**
 * Shared Gami UI — re-exports the Cyber-Brutalist primitives (originally built
 * for onboarding) so any screen can use them via `@/ui`.
 */
export * from '@/screens/Onboarding/arcade/components';
export { GAMI, FONTS } from '@/screens/Onboarding/arcade/tokens';
